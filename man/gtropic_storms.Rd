% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers_discovery.R
\name{gtropic_storms}
\alias{gtropic_storms}
\title{List available tropical cyclones}
\usage{
gtropic_storms(
  date_range = NULL,
  year = NULL,
  STORM_NAME = NULL,
  version = ":latest",
  cache = NULL
)
}
\arguments{
\item{date_range}{Optional vector of length two defining an inclusive range
of storm genesis dates. Bare years, year-month values, complete dates, and
\code{Date} or \code{POSIXct} values are accepted as described in \code{\link[=gtropic_data]{gtropic_data()}}.}

\item{year}{Optional vector of years to retrieve. Ignored when \code{date_range}
is supplied. Years outside the published collection are omitted.}

\item{STORM_NAME}{Optional character vector of storm names. Matching is
case-insensitive; every supplied value must match a published name.}

\item{version}{Dataset version to use. The default, \code{":latest"}, uses the
most recent published version. See \code{\link[=gtropic_data]{gtropic_data()}} for version formats.}

\item{cache}{Whether downloaded files may be cached. \code{NULL} uses
\code{getOption("gtropic.cache_enabled")}; \code{TRUE} or \code{FALSE} overrides that
setting for this call.}
}
\value{
A tibble containing the published metadata records for storms that
satisfy the requested genesis period and name filters. If no metadata files
are available for the selected years, a warning is issued and an empty
tibble is returned.
}
\description{
Retrieves metadata for tropical cyclones represented in G-TROPIC, including
storm identifiers, names, and genesis dates. This function can be used to
identify storms and confirm temporal coverage before retrieving exposure
data with \code{\link[=gtropic_data]{gtropic_data()}}.

Date filters are applied to \code{STORM_GENESIS_DATE_UTC}, the UTC date of the
first best-track observation. They do not select storms by landfall, closest
approach, or date of exposure.
}
\examples{
\donttest{
# Review storms that formed during the 2005 season
gtropic_storms(year = 2005)

# Retrieve the published identifiers and dates for Hurricane Katrina
gtropic_storms(STORM_NAME = "Katrina-2005")
}

}
\seealso{
\code{\link[=gtropic_data]{gtropic_data()}}, \code{\link[=gtropic_adm2]{gtropic_adm2()}}
}
